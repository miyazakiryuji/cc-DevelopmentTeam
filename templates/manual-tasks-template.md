# <feature-name> — 手動操作タスク

<!--
このファイルは architect / developer サブエージェントが生成・更新するテンプレートです。
docs/manual-tasks/<feature-name>.md として書き出されます。

「人間が UI 等で外部サービスを設定しないと進められないタスク」を一元管理するためのものです。
ソースコード側からは `// TODO(manual): docs/manual-tasks/<feature-name>.md#<anchor>` の形で参照されます。
-->

**関連仕様書:** [docs/specs/&lt;feature-name&gt;.md](../specs/<feature-name>.md)
**最終更新:** YYYY-MM-DD

---

## 全体ステータス

- 未完了タスクがある間、関連機能は完全には動作しません
- 各タスクの状態は下記参照。完了したら ステータスを「完了 (YYYY-MM-DD)」に書き換えてください

---

## タスク一覧

<!-- タスクごとに 1 セクションを使用。完了したら「☑ 完了 (YYYY-MM-DD)」に切り替える -->

### 1. <タスク名 — 例: Firebase プロジェクトの作成>

**カテゴリ:** Firebase / Supabase / OAuth / DNS / API キー登録 / 課金設定 / 証明書 / その他

**ステータス:** ☐ 未着手 (作成日: YYYY-MM-DD)

**何のために必要か:**
<このタスクが完了しないと何が動かないかを 1〜2 文で>

**手順:**
1. <アクセス先の URL や場所>
2. <UI 上での具体的な操作>
3. <出力される値や保存先>

**完了の確認方法:**
- <確認方法 1 — 例: Firebase Console に `myapp-prod` プロジェクトが見えること>
- <確認方法 2 — 例: `.env.local` に `NEXT_PUBLIC_FIREBASE_API_KEY` が設定されていること>

**関連コード（TODO 参照箇所）:**
- `<path/to/file>:<line>` — <該当行の TODO コメント要旨>

**ブロッカー（このタスクが完了しないと動かないもの）:**
- <例: ログイン機能 (login-flow) の Firebase Auth 呼び出し全般>

---

### 2. <次のタスク名>

**カテゴリ:** ...

**ステータス:** ☐ 未着手 (作成日: YYYY-MM-DD)

...

---

## 完了済みタスク（アーカイブ）

<!-- 完了したタスクはこちらに移してもよい。完了日を必ず残すこと。 -->

(まだ無し)
