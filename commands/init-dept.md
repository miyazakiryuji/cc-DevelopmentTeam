---
description: cc-DevelopmentTeam プラグインを使い始めるためにプロジェクト側を初期化する
---

このプロジェクトを 4 部署フローで運用するために、以下を順に実行してください。

## 1. 仕様書ディレクトリ

`docs/specs/` ディレクトリを作成する（既に存在する場合はスキップ）。

## 2. プロジェクトルート CLAUDE.md

プロジェクトルートに `CLAUDE.md` が無ければ作成する。
末尾に以下のセクションが **無ければ追記** する（既にあれば追記しない）。

```markdown
## 4部署フロー（cc-development-team プラグイン）

このプロジェクトは設計/開発/レビュー/テストの4部署体制で運用します。

- 設計先行: `/cc-development-team:feature <name>`
- 開発先行（back-fill 付き）: `/cc-development-team:develop <name>`
- 整合性チェック: `/cc-development-team:sync-spec`

仕様書は `docs/specs/<feature-name>.md` に集約します。
設計部署を通さずに実装した場合は、`/cc-development-team:develop` フローの末尾で必ず仕様書が逆同期されます。

部署ごとのプロジェクト固有メモは以下に置きます。各サブエージェントは起動時に自分の CLAUDE.md を読みます。

- 設計部署: `docs/dept/architect/CLAUDE.md`（ドキュメント側）
- 開発部署: `dept/developer/CLAUDE.md`（実作業側）
- レビュー部署: `dept/reviewer/CLAUDE.md`（実作業側）
- テスト部署: `dept/tester/CLAUDE.md`（実作業側）
```

## 3. 部署別 CLAUDE.md（4 ファイル）

以下のディレクトリとファイルを作成する。**既に存在するファイルは上書きしない**（ユーザーが書き込んだ内容を尊重する）。

設計部署 (`architect`) のメモは **ドキュメント系**として `docs/dept/` 配下に置き、
実作業を行う開発/レビュー/テストの 3 部署は **コードや実作業に近い場所**として
プロジェクトルート直下の `dept/` 配下に置く方針です。

### 3-1. `docs/dept/architect/CLAUDE.md`

```markdown
# architect 部署 — プロジェクト固有メモ

このファイルは architect サブエージェントが起動時に読み込みます。
プロジェクト固有の設計判断ガイドをここに書いてください。

## アーキテクチャ方針
- 採用しているアーキテクチャパターン（例: クリーンアーキテクチャ、ヘキサゴナル、MVC 等）
- 依存方向のルール
- レイヤ分割の粒度

## 仕様書の追加慣習
- このプロジェクトで仕様書に必ず含めたいセクション
- 受け入れ基準の粒度に関するルール
- 非機能要件で必ず確認する観点（性能・セキュリティ・互換性 等）

## ドメイン用語集
- ドメイン固有の語彙と定義（曖昧な略語があればここで統一する）

## 設計判断の前例集
- 過去に下した重要な設計判断と、その理由（後追いで意思決定を見直すための記録）
```

### 3-2. `dept/developer/CLAUDE.md`

```markdown
# developer 部署 — プロジェクト固有メモ

このファイルは developer サブエージェントが起動時に読み込みます。
コーディング規約・実装パターンをここに書いてください。

## コーディング規約
- 命名規約・フォーマッタ・lint ルール
- 言語/フレームワーク固有の慣習
- コメントの書き方の方針

## 実装パターン
- このプロジェクトで好まれる実装パターン
- 採用しているデザインパターン
- 避けるべきアンチパターン

## ビルド・実行コマンド
- ローカルビルド: `<コマンド>`
- 開発サーバ起動: `<コマンド>`
- 型チェック: `<コマンド>`
- フォーマッタ: `<コマンド>`

## 依存追加のルール
- 新規依存追加時の判断基準
- 利用禁止のライブラリがあれば明記
```

### 3-3. `dept/reviewer/CLAUDE.md`

```markdown
# reviewer 部署 — プロジェクト固有メモ

このファイルは reviewer サブエージェントが起動時に読み込みます。
レビュー観点・severity 判定基準をここに書いてください。

## このプロジェクトで必ず見る観点
- セキュリティ要件（特に注意するべき入力・出力経路）
- 性能要件（許容できないパターン）
- 互換性要件（後方互換・API バージョニング 等）

## severity 判定の調整
プロジェクト固有の感度に合わせて、以下の閾値を調整する。

- Critical: <このプロジェクトで Critical 扱いする例>
- High: <同上>
- Medium: <同上>
- Low: <同上>

## レビューで使う追加チェックリスト
- 標準チェックリスト（agents/reviewer.md 側）に加えて、このプロジェクトで必ず確認したい項目
```

### 3-4. `dept/tester/CLAUDE.md`

```markdown
# tester 部署 — プロジェクト固有メモ

このファイルは tester サブエージェントが起動時に読み込みます。
テスト戦略・実行コマンドをここに書いてください。

## テストフレームワーク
- ユニット: <フレームワーク名>
- 統合: <フレームワーク名>
- E2E: <フレームワーク名>

## カバレッジ目標
- 最低ライン: <% または「N/A」>
- 計測コマンド: `<コマンド>`

## テスト実行コマンド
- 全体: `<コマンド>`
- 特定ファイル: `<コマンド>`
- ウォッチモード: `<コマンド>`

## モック方針
- モックしてよいもの
- モックしてはいけないもの（DB・外部 API 等の方針）

## フレイキー対策
- フレイキーテストの一時隔離方針
- リトライポリシー
```

## 4. ユーザーへの案内

完了後、以下を簡潔に案内する:

- 作成/更新したファイル一覧
- `/cc-development-team:feature <name>` で設計先行フロー
- `/cc-development-team:develop <name>` で開発先行フロー（back-fill 付き）
- `/cc-development-team:sync-spec [name]` で整合性チェック
- 部署別の CLAUDE.md を埋めるとサブエージェントの精度が上がる旨を一言伝える
